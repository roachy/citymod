CityMod.Database = {}

function CityMod.Database:Connect(host,user,pass,database,port) end

function CityMod.Database:Query(query, callback) end

function CityMod.Database:OnConnected()
end

function CityMod.Database:OnConnectionFailed(err)
end